export interface WodanoFlight {
  arrival: string;
  departure: string;
  destination: string;
  flightNumber: string;
  id: string;
  origin: string;
  totalAmount: number;
  totalCurrency: string;
}
